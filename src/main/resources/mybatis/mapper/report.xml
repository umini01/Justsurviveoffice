<?xml version="1.0" encoding="UTF-8"?>

<!-- ==== mapper 기본설정 ==== -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="report">

	<!--중복신고여부-->
	<select id="isReported" parameterType="map" resultType="int">
	select count(*) 
	from report
	where fk_boardNo = #{fk_boardNo}
	and fk_id = #{fk_id}
	</select>

	<!--신고추가하기-->
	<insert id="insertReport" parameterType="com.spring.app.report.domain.ReportDTO">
	insert into report(reportNo, fk_id, fk_boardNo, reportReason,createdAtReport)
	values(report_seq.nextval, #{fk_id}, #{fk_boardNo}, #{reportReason}, sysdate)
	
	</insert>
</mapper>